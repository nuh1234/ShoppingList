<!doctype html>
<html lang="en">
<html>
<head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <link rel="stylesheet" type="text/css" href="todoStyles.css"> 
        <script type="text/javascript" src="todo.js"></script>
</head>
    <body id="root">
        <div class="todoContainer">
        <%if (data.length < 1) { %>
            <h3 class="welcome"> Hi <%= name %>, welcome to Nuh's TODO's, start by recording your first todo below.</h3>
        <% } else{ %> 
            <h3 class="welcome"><%= name %>'s todo list 📝</h3>
        <% } %>
            <ul>
                <% for(var i=0; i < data.length; i++) { %>
                    <%if (data[i].isDone == 0) { %>
                        <li class="itemBox">
                                <p class="item"><%= data[i].item %></p>
                                <input type="radio" id="<%= data[i].itemid%>" onclick="checkTask(this.id)"/>
                        </li>
                        <% } else{ %> 
                            <li class="itemBox">
                                    <p class="isChecked"><%= data[i].item %></p>
                                    <input type="radio" id="<%= data[i].itemid%>" checked="checked">
                            </li>  
                    <% } %>
                <% } %>
            </ul>
            <form action="/add" method='POST' class="addForm">
                <input type="text" name="added" class="addBox" placeholder="Get cat food...">
                <div style="height: 20px"></div>
                <input type="submit">
            </form>
            <div class="factBox">
                <p class="factNote">For every task you add, enjoy a random fact about cats 😸</p>
                <p class="fact">Did you know that <%= fact %> </p>
            </div>
        </div>
    </body>
</html>